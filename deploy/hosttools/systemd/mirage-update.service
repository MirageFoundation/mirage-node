[Unit]
Description=Mirage release updater tick (verify and stage)
After=docker.service
Requires=docker.service

[Service]
Type=oneshot
Nice=10
Environment=HOME=/root
ExecStart=/usr/local/bin/mirage-update --tick
