# Backend server bind address (use 127.0.0.1 for reverse proxy, 0.0.0.0 for direct)
BACKEND_HOST=127.0.0.1

# Backend server port
BACKEND_PORT=5000

# Number of Gunicorn worker processes (leave empty for auto based on CPU cores)
GUNICORN_WORKERS=

# Quest system - set to "true" to enable quests UI for all users
QUESTS_ENABLED=false

# Reward payouts - set to "true" to enable actual token transfers
# Requires REWARDS_POOL_ADDRESS to be set
PAYOUTS_ENABLED=false

# Address of the rewards pool that holds tokens for distribution
REWARDS_POOL_ADDRESS=
